[package]
name = "tools"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[[bin]]
name = "generate-token"
path = "generate_token.rs"

[dependencies]
# Shared types (canonical Claims struct)
control-center-common = { workspace = true }

jsonwebtoken = "9"