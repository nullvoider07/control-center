[package]
name = "control-center-auth"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "OAuth 2.0 and JWT authentication"
license.workspace = true

[dependencies]
# Shared types (canonical Claims struct)
control-center-common = { workspace = true }

# OAuth 2.0
oauth2 = "4.4"
reqwest = { version = "0.12", features = ["json", "rustls-tls"] }

# JWT
jsonwebtoken = "9.2"

# Async runtime
tokio = { version = "1.40", features = ["sync"] }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Utilities
uuid = { workspace = true }
anyhow = { workspace = true }
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
tokio-test = "0.4"
# #[tokio::test] needs macros + rt. Declared here rather than relying on
# feature unification from other workspace members, which made
# `cargo test -p control-center-auth` fail to compile on its own.
tokio = { version = "1.40", features = ["sync", "macros", "rt"] }

[dependencies.openssl]
version = "0.10"
optional = true

[features]
openssl-vendored = ["openssl/vendored"]